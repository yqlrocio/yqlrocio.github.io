<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Formulario</title>
</head>

<script>

//BLOQUE A
    //Presentación proactiva 
let ciudad = "Madrid"; 
const pais = "España";
let mensaje = `Hola soy de ${ciudad}, ${pais}!`;

    //Cálculo de IVA
let precio = 100;
let iva = precio * 0.21;
let total = precio + iva;
console.log(`Precio: ${precio}, IVA: ${iva}, Total: ${total}`); 

    //Conversor de Años a Días
let edad = 5;
let días = edad * 365;
console.log(`Has vivido aproximadamente ${días} días.`);


//BLOQUE B
    //El saludo personalizado
prompt("¿Cuál es tu nombre?"); 
let nombre = prompt("¿Cuál es tu nombre?");
alert(`¡Bienvenidos al curso de JS, ${nombre}!`); 

    //Suma rápida
prompt("Ingresa el primer número:");
Number(num1) = prompt("Ingresa el primer número:");
prompt("Ingresa el segundo número:");
Number(num2) = prompt("Ingresa el segundo número:");
alert(`La suma es: ${Number(num1) + Number(num2)}`);

//BLOQUE C
    //¿Eres mayor de edad?
let miEdad = prompt("¿Cuántos años tienes?");
if (edad >= 18) {
    alert("Eres mayor de edad.");
} else {
    alert("Eres menor de edad.");
}

    //Verificador de Invitados
letnombreInvitados = promt ("Introduce tu nombre:");
if (nombreInvitados === "Admin" ) {
    alert("Aceso total");
} else {
    alert("Acceso limitado");
}

    //Número impares
promt("Introduce un número:");
Number(numero) = prompt("Introduce un número:");
if (numero < 0) {
    alert("El número es negativo.");
    promt("Introduce otro número:");
    Number(numero) = prompt("Introduce otro número:");
} else {
    alert('Todos los números impares desde 1 hasta el número ingresado:');
for (let i = 1; i <= numero; i += 2) {
    console.log(i);
}
}

</script>



